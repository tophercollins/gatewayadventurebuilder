name: ttrpg_tracker
description: "TTRPG Session Tracker — record, transcribe, and analyze tabletop RPG sessions."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.5.0

  # Local Database
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0
  path_provider: ^2.1.0

  # Audio Recording
  record: ^6.0.0

  # Audio Playback
  just_audio: ^0.10.5
  just_audio_media_kit: ^2.1.0
  media_kit_libs_linux: any
  media_kit_libs_windows_audio: any

  # Backend & Sync
  supabase_flutter: ^2.0.0

  # AI/LLM
  google_generative_ai: ^0.4.0

  # Local Transcription (macOS)
  whisper_flutter_new: ^1.0.1

  # Utilities
  uuid: ^4.0.0
  flutter_secure_storage: ^9.0.0
  path: ^1.9.0
  shared_preferences: ^2.2.0
  intl: ^0.19.0
  http: ^1.2.0

  # Connectivity
  connectivity_plus: ^6.0.0

  # Routing
  go_router: ^14.0.0
  flutter_dotenv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
